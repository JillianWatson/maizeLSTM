library(keras)
library(tensorflow)

lstm <- keras_model_sequential()

# to be determined
features <- c()
look_back <- 12
forecast_horizon <- 1

input_shape <- c(look_back, length(features))





             

